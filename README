# MathParser
数式をパースして表示します。

## 開発

##  サーバサイド開発
サーバサイドはscalaのplayフレームワークを使用しています。
開発中は以下のコマンドでsbtとサーバを立ち上げておきます
> sbt run

### フロントエンド開発
フロントエンドにはvue.jsを使用しています。
開発中はfrontディレクトリにて以下コマンドを実行しwebpack-dev-serverを立ち上げておいてください。
> npm run dev

フロントエンドの開発が完了後のデプロイ時は以下のコマンドでビルドをおこなってください。
ビルド結果はpublic/frontに出力するようにしています。
> npm run build
